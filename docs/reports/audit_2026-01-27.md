# Audit Report - 2026-01-27

## Summary
- 🔴 Critical Issues: 0
- 🟡 Warnings: 2
- 🟢 Suggestions: 2

## 🟡 Warnings (Quality & Maintenance)

### 1. Leftover Debug Code (Console Logs)
- **File**: `src/components/feed/PostCard.tsx`
- **Vấn đề**: Có các dòng `console.log` phục vụ debug (ví dụ: `[Delete Post] Response...`) bị bỏ quên trong code production.
- **Tại sao cần sửa**: Làm rác console của người dùng, lộ thông tin logic xử lý không cần thiết.
- **Cách sửa**: Xóa hoặc comment lại các dòng `console.log`.

### 2. Monolithic Component (File quá lớn)
- **File**: `src/components/feed/PostCard.tsx` (~654 dòng)
- **Vấn đề**: Component `PostCard` đang làm quá nhiều việc: hiển thị post, xử lý like, comment, edit, delete, admin action, report, block user.
- **Tại sao cần sửa**: Khó bảo trì, khó đọc, dễ sinh bug khi sửa một tính năng nhỏ.
- **Cách sửa**: Tách nhỏ thành các sub-components:
  - `PostHeader` (Avatar, Name, Time, Menu)
  - `PostContent` (Text, Markdown, Image)
  - `PostActions` (Like, Comment btns)
  - `PostComments` (List comments, Input)

## 🟢 Suggestions (Optimization)

### 1. Hardcoded Community Logic
- **File**: `src/hooks/usePosts.ts`
- **Chi tiết**: Logic resolve `communitySlug` sang ID đang nằm lẫn trong hook.
- **Gợi ý**: Tách logic này ra một utility function hoặc hook riêng `useCommunityId(slug)` để tái sử dụng.

### 2. Large Hook
- **File**: `src/hooks/usePosts.ts` (~344 dòng)
- **Chi tiết**: Hook này quản lý cả Fetching, Creating, Liking, Realtime updates.
- **Gợi ý**: Có thể tách `useCreatePost` và `usePostRealtime` ra để code gọn hơn.

## Next Steps
1. Clean up `console.log`.
2. Plan refactor for `PostCard`.
